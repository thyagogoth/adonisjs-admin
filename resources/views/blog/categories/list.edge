@layout('layout.index')

@section('content')

<div class="content-wrapper pt-3">
	<section class="content-header">
		<div class="container-fluid">
			<div class="row mb-2">
				<div class="col-sm-6">
					<h1>Blog</h1>
				</div>
				<div class="col-sm-6">
					<ol class="breadcrumb float-sm-right">
						<li class="breadcrumb-item"><a href="/">Home</a></li>
						<li class="breadcrumb-item"><a href="/blog/categories">Blog</a></li>
						<li class="breadcrumb-item active">Categorias</li>
					</ol>
				</div>
			</div>
		</div>
	</section>

	<section class="content">
		<div class="row">
			<div class="col-12">
				@include('includes.notification')

				<div class="card">

					<div class="card-body">
						<a href="/blog/categories/new" class="btn btn-info">
							<i class="fas fa-plus"></i> Novo cadastro
						</a>

						<table class="datatable-list table table-bordered table-striped display">
							<thead>
								<tr>
									<th>ID</th>
									<th>Categoria</th>
									<th>Slug</th>
									<th>Data cadastro</th>
									<th>Ativo</th>
									<th>&nbsp;</th>
								</tr>
							</thead>
							<tbody>
								@each(category in categories)
								<tr>
									<td>{{ category.id }}</td>
									<td>{{ category.title }}</td>
									<td>{{ category.slug }}</td>
									<td> {{ dateFormat(category.created_at,'DD/MM/YYYY HH:mm') }}</td>
									<td>{{ getBooleanValue(category.is_active) }}</td>
									<td>
										<a href="/blog/categories/edit/{{ category.id }}" class="text-white btn btn-secondary">Editar</a>
										<a href="/blog/categories/delete/{{ category.id }}" class="text-white btn btn-danger">Excluir</a>
									</td>
								</tr>
								@endeach
							</tbody>
							<tfoot>
								<tr>
									<th>ID</th>
									<th>Categoria</th>
									<th>Slug</th>
									<th>Data cadastro</th>
									<th>Ativo</th>
									<th>&nbsp;</th>
								</tr>
							</tfoot>
						</table>
					</div>
				</div>

			</div>
		</div>
	</section>
</div>

@endsection
